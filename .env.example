# EndlessCast Configuration
# Copy this file to .env and fill in your values
# (Or run install.sh — it will generate .env automatically)

PORT=5000
ADMIN_USERNAME=admin
PASSWORD_HASH=
NODE_ENV=production

# Database (optional)
# DATABASE_URL=postgresql://user:password@localhost:5432/endlesscast
