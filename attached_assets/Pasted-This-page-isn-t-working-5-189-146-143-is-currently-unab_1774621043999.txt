This page isn’t working
5.189.146.143 is currently unable to handle this request.
HTTP ERROR 5032:56 PMInvestigated server crash causes and diagnostic stepsInvestigated server crash causes and diagnostic steps503 means the process is crashing and pm2 is restarting it in a loop. Check the logs:
bashpm2 logs endlesscast --lines 50
Most likely cause: pm2 launched it via tsx server/index.ts (dev mode) but the .env or data/ folder has a permission or missing file issue. Also check:
bash# See current status and restart count
pm2 status

# Check if port is actually bound
ss -tlnp | grep 5050

# Check the .env is correct
cat ~/EndlessCast/.env
If the restart count is climbing, the app is definitely crashing. Share the pm2 logs output and I'll pinpoint the exact error.
Quick fix to try while diagnosing — run it in foreground so you see the raw crash:
bashpm2 stop endlesscast
cd ~/EndlessCast
node dist/index.cjs
This will print the exact error to your terminal instead of hiding it in pm2 logs.root@vmi2635573:~/EndlessCast# pm2 logs endlesscast --lines 50
[TAILING] Tailing last 50 lines for [endlesscast] process (change the value with --lines option)
/root/.pm2/logs/endlesscast-out.log last 50 lines:
7|endlessc | [dotenv@17.2.3] injecting env (3) from .env -- tip: 👥 sync secrets across tpasted2:58 PMI cannot provide a summary because the user prompt is empty. Please provide the user's actual question or requestI cannot provide a summary because the user prompt is empty. Please provide the user's actual question or requestClear bug. server/static.ts uses __dirname but the file is running as ESM where it doesn't exist. Fix it directly:
bash# See the current file
cat ~/EndlessCast/server/static.ts
Then apply the fix:
bash# Add ESM __dirname shim at the top of static.ts
sed -i '1s/^/import { fileURLToPath } from "url";\nimport { dirname } from "path";\nconst __filename = fileURLToPath(import.meta.url);\nconst __dirname = dirname(__filename);\n/' ~/EndlessCast/server/static.ts
Verify it looks right:
bashhead -10 ~/EndlessCast/server/static.ts
Then rebuild and restart:
bashcd ~/EndlessCast
npm run build
pm2 restart endlesscast
pm2 logs endlesscast --lines 20
You should see the server bind to port 5050 with no more ReferenceError. If the sed approach looks messy after viewing the file, share the contents of static.ts and I'll give you the exact clean edit.7|endlessc |     at <anonymous> (/root/EndlessCast/server/index.ts:112:5)
7|endlessc | UNHANDLED REJECTION: ReferenceError: __dirname is not defined
7|endlessc |     at serveStatic (/root/EndlessCast/server/static.ts:6:33)
7|endlessc |     at <anonymous> (/root/EndlessCast/server/index.ts:112:5)
7|epasted3:15 PMThe user prompt is empty, so I cannot determine the primary language. However, based on the thinking block being in English, here is the summary:

Diagnosed ESM compatibility issue and orchestrated direct file repair strategyThe user prompt is empty, so I cannot determine the primary language. However, based on the thinking block being in English, here is the summary:

Diagnosed ESM compatibility issue and orchestrated direct file repair strategyThe sed fix was never applied — the file still has __dirname. Also the git pull is blocked by a local change to start.sh. Fix everything in sequence:
bash# 1. Stash the local start.sh change, pull, then restore
cd ~/EndlessCast
git stash
git pull origin main
git stash pop

# 2. Stop the crashing process
pm2 delete endlesscast

# 3. Fix static.ts — replace __dirname with ESM-compatible version
cat > /root/EndlessCast/server/static.ts << 'EOF'
import express, { type Express } from "express";
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";
import { dirname } from "path";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

export function serveStatic(app: Express) {
  const distPath = path.resolve(__dirname, "public");
  if (!fs.existsSync(distPath)) {
    throw new Error(
      `Could not find the build directory: ${distPath}, make sure to build the client first`,
    );
  }

  app.use(express.static(distPath));

  // fall through to index.html if the file doesn't exist
  app.use("*", (_req, res) => {
    res.sendFile(path.resolve(distPath, "index.html"));
  });
}
EOF

# 4. Rebuild
npm run build

# 5. Restart
./start.sh
Then verify:
bashsleep 3 && pm2 logs endlesscast --lines 10
You should now see Server running on port 5050 with no __dirname errors. If the git stash pop causes a conflict on start.sh, just run git checkout start.sh to reset it — the installer already generated the correct one for your setup.root@vmi2635573:~/EndlessCast# # 1. Stash the local start.sh change, pull, then restore
cd ~/EndlessCast
git stash
git pull origin main
git stash pop

# 2. Stop the crashing process
pm2 delete endlesscast

# 3. Fix static.ts — replace __dirname with ESM-compatible version
cat > /root/EndlessCast/serpasted3:16 PMI cannot provide a summary because the user prompt is empty. Please provide the user's actual question or requestI cannot provide a summary because the user prompt is empty. Please provide the user's actual question or requestTwo issues to fix: the start.sh merge conflict, and the real root cause — pm2 is running tsx (ESM mode) but the fix used import.meta which only works in ESM. Since the build outputs CJS where __dirname IS natively available, the right fix is to run the built file instead of tsx.
bash# 1. Fix start.sh by taking the upstream version
git checkout start.sh

# 2. Revert static.ts back to original (__dirname works fine in CJS)
cat > /root/EndlessCast/server/static.ts << 'EOF'
import express, { type Express } from "express";
import fs from "fs";
import path from "path";

export function serveStatic(app: Express) {
  const distPath = path.resolve(__dirname, "public");
  if (!fs.existsSync(distPath)) {
    throw new Error(
      `Could not find the build directory: ${distPath}, make sure to build the client first`,
    );
  }

  app.use(express.static(distPath));

  app.use("*", (_req, res) => {
    res.sendFile(path.resolve(distPath, "index.html"));
  });
}
EOF

# 3. Rebuild
npm run build

# 4. Start pm2 pointing at the built CJS file (not tsx)
pm2 start /root/EndlessCast/dist/index.cjs --name endlesscast
pm2 save

# 5. Verify
sleep 3 && pm2 logs endlesscast --lines 15
The key insight: tsx runs TypeScript as ESM → __dirname breaks. But dist/index.cjs is compiled CommonJS → __dirname works natively. No shim needed.